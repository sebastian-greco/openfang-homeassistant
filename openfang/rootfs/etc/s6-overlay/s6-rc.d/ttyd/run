#!/command/with-contenv bashio
# shellcheck shell=bash
[ "${ENABLE_TERMINAL}" = "true" ] || { bashio::log.info "ttyd disabled (enable_terminal=false)"; exec sleep infinity; }
exec ttyd -W -i 127.0.0.1 -p 7681 -b /terminal bash
