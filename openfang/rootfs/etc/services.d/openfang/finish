#!/bin/sh
EXIT_CODE="${1}"
EXIT_SIGNAL="${2}"
rm -f /data/.openfang/daemon.json 2>/dev/null || true
if [ "${EXIT_CODE}" -ne 256 ] && [ "${EXIT_CODE}" -ne 0 ]; then
    echo "[ERROR] openfang crashed (exit_code=${EXIT_CODE} signal=${EXIT_SIGNAL}), s6 will restart it"
fi
